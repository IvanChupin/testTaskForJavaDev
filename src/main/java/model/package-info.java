@XmlSchema(
        xmlns = {
                @XmlNs(prefix = "urn", namespaceURI = "wsapi:Payment"),
                @XmlNs(prefix = "uts", namespaceURI = "wsapi:Utils")
        })
package model;

import javax.xml.bind.annotation.XmlNs;
import javax.xml.bind.annotation.XmlSchema;

